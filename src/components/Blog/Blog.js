/* eslint-disable */
import React from 'react';
import './Blog.scss';

const Blog = ({ blogList }) => {
    console.log(blogList);
    return (
        <div>
            Hello
        </div>
    );
}

export default Blog;